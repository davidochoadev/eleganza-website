# Telegram (per l'invio delle richieste dal listino prezzi)
# Crea un bot con @BotFather e ottieni il token.
# Per CHAT_IDS: avvia una chat con il bot, poi visita:
# https://api.telegram.org/bot<TOKEN>/getUpdates e copia "chat":{"id": ...}
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_IDS=

# Listino prezzi da Laravel (opzionale)
# Se impostate, la pagina /servizi carica il listino dall'API Laravel invece che da src/data/services.json
# Endpoint: {LARAVEL_PRICE_LIST_BASE_URL}/price-list-feed/{PRICE_LIST_FEED_SLUG}?token={PRICE_LIST_FEED_TOKEN}
LARAVEL_PRICE_LIST_BASE_URL=
PRICE_LIST_FEED_SLUG=
PRICE_LIST_FEED_TOKEN=
